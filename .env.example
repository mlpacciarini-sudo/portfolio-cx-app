# Copiá este archivo a ".env" (no versionado) y completá el valor real.
# El token NUNCA debe hardcodearse ni viajar al bundle del frontend:
# solo lo lee este archivo, en Node, para el test de Playwright "live".
CLICKUP_API_TOKEN=
